# Supabase Configuration
# Get these from your Supabase project dashboard: Settings > API

# Your Supabase project URL
NEXT_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co

# Your Supabase anonymous/public key (safe to use in browser)
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Optional: Service role key for admin operations (NEVER expose to client!)
# Only use in server-side code when absolutely necessary
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Your site URL (for OAuth callbacks)
# Development: http://localhost:3000
# Production: https://yourdomain.com
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# =============================================
# Setup Instructions:
# =============================================
# 1. Create a new Supabase project at https://supabase.com
# 2. Run the SQL from supabase/schema.sql in SQL Editor
# 3. Go to Settings > API to get your URL and keys
# 4. Copy this file to .env.local (not .env) and fill in your values.
#    Next.js loads .env.local for local dev; middleware may not see .env.
# 5. Configure Google OAuth:
#    - Go to Authentication > Providers in Supabase Dashboard
#    - Enable Google provider
#    - Add Client ID & Secret from Google Cloud Console
#    - See: https://supabase.com/docs/guides/auth/social-login/auth-google
